# C

## 헤더 파일 패키지

```sh
apt install \
  build-essential \
  linux-headers-$(uname -r)
  # libssl-dev \
  # libreadline-dev \
  # zlib1g-dev \
  # libbz2-dev \
```

## Compiler

### Clang (LLVM)

> 클랭이라고 발음한다.

```sh
sudo apt install clang
```

### GCC (GNU Compiler Collection)

```sh
sudo apt install gcc
```
